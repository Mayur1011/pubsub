In traditional queues, the broker pushes messages to consumers and deletes them the moment they are acknowledged.
In our log-structured architecture, the server treats messages as a permanent, read-only history log. The server doesn't care who is reading or how fast they are reading. The consumer is entirely in the driver's seat. It is responsible for pulling data and tracking its own position


put related data in same paritition and assign a thread(consumer) from the consumer group to a fixed partition, so no two consumers read from the same partition at the same time.


As the message will be of variable lengths, we need to store the offest to byte mapping in a .index file.
So when a consumer tell the server to give data from offset X, the server will look up the byte offset in the .index file and return the data from that offset.


Producer will produce a entry for a topic
1. Get the partition for the topic (topic/parition_i)
2. Append the entry to the partition log
3. Update the .index file with the new offset to byte mapping



1. Network Card ----> Receives raw TCP bytes from client.
2. Server Layer ----> Parses Request Header, extracts ProducePayload (raw vector bytes).
3. Server Layer ----> Calls the Phase 1 deserializeRecordBatch(payload.rawRecordBatch).
4. Storage Layer ----> Calls Partition::append(batch).
                        - Overwrites batch.baseOffset with nextRecordOffset.
                        - Advances nextRecordOffset by batch.numRecords.
                        - Calls serializeRecordBatch(batch) to get final corrected bytes.
                        - Appends those bytes to the active .log file.


1. Why not directly store the struct in the log file and do serialization/deserialization? Our processor used little endian byte order to store data but the network byte order is big endian.
2. in epoll kernel wakes up the managing thread in event loop when there is some work to dos. so a single thread can handle multiple connections concurrently. epoll does not change the number of sockets. It changes how you wait for events on those sockets.
               +----------------+
               |   accept()      |
               +--------+--------+
                        |
                 New client socket
                        |
                        v
                Register with epoll
                        |
        +---------------+---------------+
        |               |               |
      fd4             fd5            fd6
        \               |              /
         \              |             /
          +--------------------------+
          |      epoll_wait()        |
          |     (1-4 I/O threads)    |
          +--------------------------+
                     |
         --------------------------
         |                        |
   Simple request          Heavy computation
         |                        |
     send reply             Thread Pool
                                   |
                            send reply later

------------------------------------------------------------------------------------------------------
1. i have written the logic to serialize and deserialize the structs so i can store the serialized data in the .log file
2. Now each partition will have multiple segments (easy to search) as we can skip segments if out record is not in that segment. Also easier to delete records easily if we modularize them.
we are giving an illusion that we are storing everything in a single .log file but we make segments for easy access to records.
Now i have done with the storage part i can store RecordBatches in segment file and read them using the index files

Now moving to networking part so the client can send RecordBatches to the server and the server can store them in the .log file.